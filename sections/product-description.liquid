<section class="flat-spacing pt-0 color-{{ section.settings.color_scheme }} section-{{ section.id }}-padding">
    <div class="{{ section.settings.width }}">
        {% if section.settings.layout == 'accordion' %}
            <!-- Accordion Layout -->
            {% for block in section.blocks %}
                {% case block.type %}
                    {% when 'description' %}
                        {% if product.description != blank %}
                        <div class="widget-accordion wd-product-descriptions" {{ block.shopify_attributes }}>
                            <div class="accordion-title collapsed" data-bs-target="#description-{{ block.id }}" data-bs-toggle="collapse" aria-expanded="true" aria-controls="description-{{ block.id }}" role="button">
                                <span>{{ block.settings.title }}</span>
                                <span class="icon icon-arrow-down"></span>
                            </div>
                            <div id="description-{{ block.id }}" class="collapse">
                                <div class="accordion-body widget-desc">
                                    <span>{{ product.description }}</span>
                                </div>
                            </div>
                        </div>
                        {% endif %}

                    {% when 'materials' %}
                        {% if product.metafields.custom.materials_care != blank %}
                        <div class="widget-accordion wd-product-descriptions" {{ block.shopify_attributes }}>
                            <div class="accordion-title collapsed" data-bs-target="#materials-{{ block.id }}" data-bs-toggle="collapse" aria-expanded="true" aria-controls="materials-{{ block.id }}" role="button">
                                <span>{{ block.settings.title }}</span>
                                <span class="icon icon-arrow-down"></span>
                            </div>
                            <div id="materials-{{ block.id }}" class="collapse">
                                <div class="accordion-body widget-material">
                                    <div class="item">
                                        <p class="fw-medium title">{{ product.metafields.custom.materials_care.value.first }}</p>
                                        <ul>
                                            {% for care_instruction in product.metafields.custom.materials_care.value offset:1 %}
                                                <li>{{ care_instruction }}</li>
                                            {% endfor %}
                                        </ul>
                                    </div>
                                </div>
                            </div>
                        </div>
                        {% endif %}

                    {% when 'return_policies' %}
                        <div class="widget-accordion wd-product-descriptions" {{ block.shopify_attributes }}>
                            <div class="accordion-title collapsed" data-bs-target="#returnPolicies-{{ block.id }}" data-bs-toggle="collapse" aria-expanded="true" aria-controls="returnPolicies-{{ block.id }}" role="button">
                                <span>{{ block.settings.title }}</span>
                                <span class="icon icon-arrow-down"></span>
                            </div>
                            <div id="returnPolicies-{{ block.id }}" class="collapse">
                                <div class="accordion-body">
                                    {% if block.settings.policies != blank %}
                                    <ul class="list-policies">
                                        {% assign policies = block.settings.policies | split: '
' %}
                                        {% for policy in policies %}
                                            {% if policy != blank %}
                                            <li>
                                                {{ policy | default: block.settings.default_icon }}
                                            </li>
                                            {% endif %}
                                        {% endfor %}
                                    </ul>
                                    {% endif %}
                                    {% if block.settings.note != blank %}
                                    <p class="text-center text-paragraph">{{ block.settings.note }}</p>
                                    {% endif %}
                                </div>
                            </div>
                        </div>

                    {% when 'additional_info' %}
                        {% if product.metafields.custom.additional_information != blank or block.settings.show_default_info %}
                        <div class="widget-accordion wd-product-descriptions" {{ block.shopify_attributes }}>
                            <div class="accordion-title collapsed" data-bs-target="#addInformation-{{ block.id }}" data-bs-toggle="collapse" aria-expanded="true" aria-controls="addInformation-{{ block.id }}" role="button">
                                <span>{{ block.settings.title }}</span>
                                <span class="icon icon-arrow-down"></span>
                            </div>
                            <div id="addInformation-{{ block.id }}" class="collapse">
                                <div class="accordion-body">
                                    <table class="tb-info-product text-md">
                                        <tbody>
                                            {% if product.metafields.custom.additional_information != blank %}
                                                {% for info in product.metafields.custom.additional_information.value %}
                                                    <tr class="tb-attr-item">
                                                        <th class="tb-attr-label">{{ info.label }}</th>
                                                        <td class="tb-attr-value">
                                                            <p>{{ info.value }}</p>
                                                        </td>
                                                    </tr>
                                                {% endfor %}
                                            {% else %}
                                                {% if product.vendor != blank %}
                                                <tr class="tb-attr-item">
                                                    <th class="tb-attr-label">{{ block.settings.brand_label }}</th>
                                                    <td class="tb-attr-value">
                                                        <p>{{ product.vendor }}</p>
                                                    </td>
                                                </tr>
                                                {% endif %}
                                                
                                                {% if product.type != blank %}
                                                <tr class="tb-attr-item">
                                                    <th class="tb-attr-label">{{ block.settings.type_label }}</th>
                                                    <td class="tb-attr-value">
                                                        <p>{{ product.type }}</p>
                                                    </td>
                                                </tr>
                                                {% endif %}
                                                
                                                {% if product.tags.size > 0 %}
                                                <tr class="tb-attr-item">
                                                    <th class="tb-attr-label">{{ block.settings.tags_label }}</th>
                                                    <td class="tb-attr-value">
                                                        <p>{{ product.tags | join: ', ' }}</p>
                                                    </td>
                                                </tr>
                                                {% endif %}
                                            {% endif %}
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                        {% endif %}

                    {% when 'reviews' %}
                        <div class="widget-accordion wd-product-descriptions" {{ block.shopify_attributes }}>
                            <div class="accordion-title collapsed" data-bs-target="#reviews-{{ block.id }}" data-bs-toggle="collapse" aria-expanded="true" aria-controls="reviews-{{ block.id }}" role="button">
                                <span>{{ block.settings.title }}</span>
                                <span class="icon icon-arrow-down"></span>
                            </div>
                            <div id="reviews-{{ block.id }}" class="collapse">
                                <div class="accordion-body wd-customer-review">
                                    <div class="review-heading">
                                        <h6 class="title">{{ block.settings.customer_review_title }}</h6>
                                        <div class="box-rate-review">
                                            <div class="rating-summary">
                                                <ul class="list-star">
                                                    {% for i in (1..5) %}
                                                        <li><i class="icon icon-star"></i></li>
                                                    {% endfor %}
                                                    <li><span class="count-star text-md">({{ product.reviews_count }})</span></li>
                                                </ul>
                                                <span class="text-md rating-average">{{ product.reviews_average }}/5.0</span>
                                            </div>
                                            {% if product.metafields.custom.rating_breakdown != blank %}
                                            <div class="rating-breakdown">
                                                {% for rating in product.metafields.custom.rating_breakdown.value %}
                                                <div class="rating-breakdown-item">
                                                    <div class="rating-score">
                                                        {{ rating.stars }} <i class="icon icon-star"></i>
                                                    </div>
                                                    <div class="rating-bar">
                                                        <div class="value" style="width: {{ rating.percentage }}%;"></div>
                                                    </div>
                                                    <span class="rating-count">{{ rating.count }}</span>
                                                </div>
                                                {% endfor %}
                                            </div>
                                            {% endif %}
                                        </div>
                                        <a href="#form-review-{{ block.id }}" class="tf-btn btn-dark2 animate-btn">{{ block.settings.write_review_text }}</a>
                                    </div>
                                    
                                    {% if product.reviews != blank %}
                                    <div class="review-section">
                                        <ul class="review-list">
                                            {% for review in product.reviews limit: block.settings.max_reviews_display %}
                                            <li class="review-item">
                                                <div class="review-avt">
                                                    {% if review.author_image != blank %}
                                                        <img src="{{ review.author_image }}" alt="{{ review.author }}" width="40" height="40">
                                                    {% else %}
                                                        <div class="review-avatar-placeholder">{{ review.author | first | upcase }}</div>
                                                    {% endif %}
                                                </div>
                                                <div class="review-content">
                                                    <div class="review-info">
                                                        <div class="review-meta">
                                                            <span class="review-author fw-medium text-md">{{ review.author }}</span>
                                                            <span class="review-date text-sm">{{ review.created_at | date: '%b %d, %Y' }}</span>
                                                        </div>
                                                        <div class="list-star star-{{ review.rating }}">
                                                            {% for i in (1..5) %}
                                                                <i class="icon icon-star"></i>
                                                            {% endfor %}
                                                        </div>
                                                    </div>
                                                    <p class="text text-sm text-main-4">{{ review.body }}</p>
                                                </div>
                                            </li>
                                            {% endfor %}
                                        </ul>
                                    </div>
                                    {% endif %}
                                    
                                    {% if block.settings.show_review_form %}
                                    <form id="form-review-{{ block.id }}" action="{{ routes.root_url }}apps/reviews" method="post" class="form-review">
                                        <h6 class="title">{{ block.settings.write_review_title }}</h6>
                                        <p class="note text-md text-main-4">{{ block.settings.review_form_note }}</p>
                                        
                                        <input type="hidden" name="product_id" value="{{ product.id }}">
                                        
                                        <div class="box-rating">
                                            <span class="text-md">{{ block.settings.your_rating_text }}</span>
                                            <div class="list-rating-check">
                                                {% for i in (1..5) %}
                                                <input type="radio" id="star{{ i }}-{{ block.id }}" name="rating" value="{{ i }}" required>
                                                <label for="star{{ i }}-{{ block.id }}" title="{{ i }} star"></label>
                                                {% endfor %}
                                            </div>
                                        </div>
                                        
                                        <div class="group-2-ip">
                                            <input type="text" name="author" placeholder="{{ block.settings.name_placeholder }}" required>
                                            <input type="email" name="email" placeholder="{{ block.settings.email_placeholder }}" required>
                                        </div>
                                        
                                        <textarea name="body" id="note-{{ block.id }}" placeholder="{{ block.settings.review_placeholder }}" required></textarea>
                                        
                                        {% if block.settings.show_save_checkbox %}
                                        <div class="check-save">
                                            <input type="checkbox" class="tf-check" id="checksave-{{ block.id }}" name="save_info">
                                            <label for="checksave-{{ block.id }}" class="label text-md">{{ block.settings.save_checkbox_text }}</label>
                                        </div>
                                        {% endif %}
                                        
                                        <button type="submit" class="tf-btn animate-btn">{{ block.settings.submit_button_text }}</button>
                                    </form>
                                    {% endif %}
                                </div>
                            </div>
                        </div>

                    {% when 'custom_content' %}
                        <div class="widget-accordion wd-product-descriptions" {{ block.shopify_attributes }}>
                            <div class="accordion-title collapsed" data-bs-target="#custom-{{ block.id }}" data-bs-toggle="collapse" aria-expanded="true" aria-controls="custom-{{ block.id }}" role="button">
                                <span>{{ block.settings.title }}</span>
                                <span class="icon icon-arrow-down"></span>
                            </div>
                            <div id="custom-{{ block.id }}" class="collapse">
                                <div class="accordion-body">
                                    {{ block.settings.content }}
                                </div>
                            </div>
                        </div>
                {% endcase %}
            {% endfor %}

        {% elsif section.settings.layout == 'vertical_tab' %}
            <!-- Vertical Tab Layout -->
            <div class="flat-animate-tab tab-vertical-product-desc">
                <ul class="menu-tab" role="tablist">
                    {% for block in section.blocks %}
                        {% case block.type %}
                            {% when 'description' %}
                                {% if product.description != blank %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#descriptions-{{ block.id }}" class="tab-link {% if forloop.first %}active{% endif %}" data-bs-toggle="tab" aria-selected="{% if forloop.first %}true{% else %}false{% endif %}" role="tab">{{ block.settings.title }}</a>
                                </li>
                                {% endif %}
                            {% when 'materials' %}
                                {% if product.metafields.custom.materials_care != blank %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#materials-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                                {% endif %}
                            {% when 'return_policies' %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#returnPolicies-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                            {% when 'additional_info' %}
                                {% if product.metafields.custom.additional_information != blank or block.settings.show_default_info %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#addInformation-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                                {% endif %}
                            {% when 'reviews' %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#reviews-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                            {% when 'custom_content' %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#custom-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                        {% endcase %}
                    {% endfor %}
                </ul>
                
                <div class="tab-content">
                    {% for block in section.blocks %}
                        {% case block.type %}
                            {% when 'description' %}
                                {% if product.description != blank %}
                                <div class="tab-pane {% if forloop.first %}active show{% endif %} wd-product-descriptions" id="descriptions-{{ block.id }}" role="tabpanel">
                                    <div class="item">
                                        <span>{{ product.description }}</span>
                                    </div>
                                </div>
                                {% endif %}

                            {% when 'materials' %}
                                {% if product.metafields.custom.materials_care != blank %}
                                <div class="tab-pane wd-product-descriptions" id="materials-{{ block.id }}" role="tabpanel">
                                    <div class="item">
                                        <p class="fw-medium title">{{ block.settings.subtitle }}</p>
                                        <ul>
                                            {% for care_instruction in product.metafields.custom.materials_care.value %}
                                                <li>{{ care_instruction }}</li>
                                            {% endfor %}
                                        </ul>
                                    </div>
                                </div>
                                {% endif %}

                            {% when 'return_policies' %}
                                <div class="tab-pane wd-product-descriptions" id="returnPolicies-{{ block.id }}" role="tabpanel">
                                    {% if block.settings.policies != blank %}
                                    <ul class="list-policies">
                                        {% assign policies = block.settings.policies | split: '
' %}
                                        {% for policy in policies %}
                                            {% if policy != blank %}
                                            <li>
                                                {{ policy | default: block.settings.default_icon }}
                                            </li>
                                            {% endif %}
                                        {% endfor %}
                                    </ul>
                                    {% endif %}
                                    {% if block.settings.note != blank %}
                                    <p class="text-center text-paragraph">{{ block.settings.note }}</p>
                                    {% endif %}
                                </div>

                            {% when 'additional_info' %}
                                {% if product.metafields.custom.additional_information != blank or block.settings.show_default_info %}
                                <div class="tab-pane wd-product-descriptions wd-vertical-addInformation" id="addInformation-{{ block.id }}" role="tabpanel">
                                    <table class="tb-info-product text-md">
                                        <tbody>
                                            {% if product.metafields.custom.additional_information != blank %}
                                                {% for info in product.metafields.custom.additional_information.value %}
                                                    <tr class="tb-attr-item">
                                                        <th class="tb-attr-label">{{ info.label }}</th>
                                                        <td class="tb-attr-value">
                                                            <p>{{ info.value }}</p>
                                                        </td>
                                                    </tr>
                                                {% endfor %}
                                            {% else %}
                                                {% if product.vendor != blank %}
                                                <tr class="tb-attr-item">
                                                    <th class="tb-attr-label">{{ block.settings.brand_label }}</th>
                                                    <td class="tb-attr-value">
                                                        <p>{{ product.vendor }}</p>
                                                    </td>
                                                </tr>
                                                {% endif %}
                                                
                                                {% if product.type != blank %}
                                                <tr class="tb-attr-item">
                                                    <th class="tb-attr-label">{{ block.settings.type_label }}</th>
                                                    <td class="tb-attr-value">
                                                        <p>{{ product.type }}</p>
                                                    </td>
                                                </tr>
                                                {% endif %}
                                                
                                                {% if product.tags.size > 0 %}
                                                <tr class="tb-attr-item">
                                                    <th class="tb-attr-label">{{ block.settings.tags_label }}</th>
                                                    <td class="tb-attr-value">
                                                        <p>{{ product.tags | join: ', ' }}</p>
                                                    </td>
                                                </tr>
                                                {% endif %}
                                            {% endif %}
                                        </tbody>
                                    </table>
                                </div>
                                {% endif %}

                            {% when 'reviews' %}
                                <div class="tab-pane wd-product-descriptions wd-vertical-review" id="reviews-{{ block.id }}" role="tabpanel">
                                    <div class="wd-customer-review">
                                        <div class="review-heading">
                                            <h6 class="title">{{ block.settings.customer_review_title }}</h6>
                                            <div class="box-rate-review">
                                                <div class="rating-summary">
                                                    <ul class="list-star">
                                                        {% for i in (1..5) %}
                                                            <li><i class="icon icon-star"></i></li>
                                                        {% endfor %}
                                                        <li><span class="count-star text-md">({{ product.reviews_count }})</span></li>
                                                    </ul>
                                                    <span class="text-md rating-average">{{ product.reviews_average }}/5.0</span>
                                                </div>
                                                {% if product.metafields.custom.rating_breakdown != blank %}
                                                <div class="rating-breakdown">
                                                    {% for rating in product.metafields.custom.rating_breakdown.value %}
                                                    <div class="rating-breakdown-item">
                                                        <div class="rating-score">
                                                            {{ rating.stars }} <i class="icon icon-star"></i>
                                                        </div>
                                                        <div class="rating-bar">
                                                            <div class="value" style="width: {{ rating.percentage }}%;"></div>
                                                        </div>
                                                        <span class="rating-count">{{ rating.count }}</span>
                                                    </div>
                                                    {% endfor %}
                                                </div>
                                                {% endif %}
                                            </div>
                                            <a href="#form-review-{{ block.id }}" class="tf-btn btn-dark2 animate-btn">{{ block.settings.write_review_text }}</a>
                                        </div>
                                        
                                        {% if product.reviews != blank %}
                                        <div class="review-section">
                                            <ul class="review-list">
                                                {% for review in product.reviews limit: block.settings.max_reviews_display %}
                                                <li class="review-item">
                                                    <div class="review-avt">
                                                        {% if review.author_image != blank %}
                                                            <img src="{{ review.author_image }}" alt="{{ review.author }}" width="40" height="40">
                                                        {% else %}
                                                            <div class="review-avatar-placeholder">{{ review.author | first | upcase }}</div>
                                                        {% endif %}
                                                    </div>
                                                    <div class="review-content">
                                                        <div class="review-info">
                                                            <div class="review-meta">
                                                                <span class="review-author fw-medium text-md">{{ review.author }}</span>
                                                                <span class="review-date text-sm">{{ review.created_at | date: '%b %d, %Y' }}</span>
                                                            </div>
                                                            <div class="list-star star-{{ review.rating }}">
                                                                {% for i in (1..5) %}
                                                                    <i class="icon icon-star"></i>
                                                                {% endfor %}
                                                            </div>
                                                        </div>
                                                        <p class="text text-sm text-main-4">{{ review.body }}</p>
                                                    </div>
                                                </li>
                                                {% endfor %}
                                            </ul>
                                        </div>
                                        {% endif %}
                                        
                                        {% if block.settings.show_review_form %}
                                        <form id="form-review-{{ block.id }}" action="{{ routes.root_url }}apps/reviews" method="post" class="form-review">
                                            <h6 class="title">{{ block.settings.write_review_title }}</h6>
                                            <p class="note text-md text-main-4">{{ block.settings.review_form_note }}</p>
                                            
                                            <input type="hidden" name="product_id" value="{{ product.id }}">
                                            
                                            <div class="box-rating">
                                                <span class="text-md">{{ block.settings.your_rating_text }}</span>
                                                <div class="list-rating-check">
                                                    {% for i in (1..5) %}
                                                    <input type="radio" id="star{{ i }}-{{ block.id }}" name="rating" value="{{ i }}" required>
                                                    <label for="star{{ i }}-{{ block.id }}" title="{{ i }} star"></label>
                                                    {% endfor %}
                                                </div>
                                            </div>
                                            
                                            <div class="group-2-ip">
                                                <input type="text" name="author" placeholder="{{ block.settings.name_placeholder }}" required>
                                                <input type="email" name="email" placeholder="{{ block.settings.email_placeholder }}" required>
                                            </div>
                                            
                                            <textarea name="body" id="note-{{ block.id }}" placeholder="{{ block.settings.review_placeholder }}" required></textarea>
                                            
                                            {% if block.settings.show_save_checkbox %}
                                            <div class="check-save">
                                                <input type="checkbox" class="tf-check" id="checksave-{{ block.id }}" name="save_info">
                                                <label for="checksave-{{ block.id }}" class="label text-md">{{ block.settings.save_checkbox_text }}</label>
                                            </div>
                                            {% endif %}
                                            
                                            <button type="submit" class="tf-btn animate-btn">{{ block.settings.submit_button_text }}</button>
                                        </form>
                                        {% endif %}
                                    </div>
                                </div>

                            {% when 'custom_content' %}
                                <div class="tab-pane wd-product-descriptions" id="custom-{{ block.id }}" role="tabpanel">
                                    {{ block.settings.content }}
                                </div>
                        {% endcase %}
                    {% endfor %}
                </div>
            </div>

        {% else %}
            <!-- Horizontal Centered Tab Layout -->
            <div class="flat-animate-tab tab-product-desc">
                <ul class="menu-tab justify-content-md-center" role="tablist">
                    {% for block in section.blocks %}
                        {% case block.type %}
                            {% when 'description' %}
                                {% if product.description != blank %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#descriptions-{{ block.id }}" class="tab-link {% if forloop.first %}active{% endif %}" data-bs-toggle="tab" aria-selected="{% if forloop.first %}true{% else %}false{% endif %}" role="tab">{{ block.settings.title }}</a>
                                </li>
                                {% endif %}
                            {% when 'materials' %}
                                {% if product.metafields.custom.materials_care != blank %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#materials-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                                {% endif %}
                            {% when 'return_policies' %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#returnPolicies-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                            {% when 'additional_info' %}
                                {% if product.metafields.custom.additional_information != blank or block.settings.show_default_info %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#addInformation-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                                {% endif %}
                            {% when 'reviews' %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#reviews-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                            {% when 'custom_content' %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#custom-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                        {% endcase %}
                    {% endfor %}
                </ul>
                
                <div class="tab-content">
                    {% for block in section.blocks %}
                        {% case block.type %}
                            {% when 'description' %}
                                {% if product.description != blank %}
                                <div class="tab-pane {% if forloop.first %}active show{% endif %} wd-product-descriptions" id="descriptions-{{ block.id }}" role="tabpanel">
                                    <div class="item">
                                        <span>{{ product.description }}</span>
                                    </div>
                                </div>
                                {% endif %}

                            {% when 'materials' %}
                                {% if product.metafields.custom.materials_care != blank %}
                                <div class="tab-pane wd-product-descriptions" id="materials-{{ block.id }}" role="tabpanel">
                                    <div class="item">
                                        <p class="fw-medium title">{{ block.settings.subtitle }}</p>
                                        <ul>
                                            {% for care_instruction in product.metafields.custom.materials_care.value %}
                                                <li>{{ care_instruction }}</li>
                                            {% endfor %}
                                        </ul>
                                    </div>
                                </div>
                                {% endif %}

                            {% when 'return_policies' %}
                                <div class="tab-pane wd-product-descriptions" id="returnPolicies-{{ block.id }}" role="tabpanel">
                                    {% if block.settings.policies != blank %}
                                    <ul class="list-policies">
                                        {% assign policies = block.settings.policies | split: '
' %}
                                        {% for policy in policies %}
                                            {% if policy != blank %}
                                            <li>
                                                {{ policy | default: block.settings.default_icon }}
                                            </li>
                                            {% endif %}
                                        {% endfor %}
                                    </ul>
                                    {% endif %}
                                    {% if block.settings.note != blank %}
                                    <p class="text-center text-paragraph">{{ block.settings.note }}</p>
                                    {% endif %}
                                </div>

                            {% when 'additional_info' %}
                                {% if product.metafields.custom.additional_information != blank or block.settings.show_default_info %}
                                <div class="tab-pane wd-product-descriptions wd-vertical-addInformation" id="addInformation-{{ block.id }}" role="tabpanel">
                                    <table class="tb-info-product text-md">
                                        <tbody>
                                            {% if product.metafields.custom.additional_information != blank %}
                                                {% for info in product.metafields.custom.additional_information.value %}
                                                    <tr class="tb-attr-item">
                                                        <th class="tb-attr-label">{{ info.label }}</th>
                                                        <td class="tb-attr-value">
                                                            <p>{{ info.value }}</p>
                                                        </td>
                                                    </tr>
                                                {% endfor %}
                                            {% else %}
                                                {% if product.vendor != blank %}
                                                <tr class="tb-attr-item">
                                                    <th class="tb-attr-label">{{ block.settings.brand_label }}</th>
                                                    <td class="tb-attr-value">
                                                        <p>{{ product.vendor }}</p>
                                                    </td>
                                                </tr>
                                                {% endif %}
                                                
                                                {% if product.type != blank %}
                                                <tr class="tb-attr-item">
                                                    <th class="tb-attr-label">{{ block.settings.type_label }}</th>
                                                    <td class="tb-attr-value">
                                                        <p>{{ product.type }}</p>
                                                    </td>
                                                </tr>
                                                {% endif %}
                                                
                                                {% if product.tags.size > 0 %}
                                                <tr class="tb-attr-item">
                                                    <th class="tb-attr-label">{{ block.settings.tags_label }}</th>
                                                    <td class="tb-attr-value">
                                                        <p>{{ product.tags | join: ', ' }}</p>
                                                    </td>
                                                </tr>
                                                {% endif %}
                                            {% endif %}
                                        </tbody>
                                    </table>
                                </div>
                                {% endif %}

                            {% when 'reviews' %}
                                <div class="tab-pane wd-product-descriptions wd-vertical-review" id="reviews-{{ block.id }}" role="tabpanel">
                                    <div class="wd-customer-review">
                                        <div class="review-heading">
                                            <h6 class="title">{{ block.settings.customer_review_title }}</h6>
                                            <div class="box-rate-review">
                                                <div class="rating-summary">
                                                    <ul class="list-star">
                                                        {% for i in (1..5) %}
                                                            <li><i class="icon icon-star"></i></li>
                                                        {% endfor %}
                                                        <li><span class="count-star text-md">({{ product.reviews_count }})</span></li>
                                                    </ul>
                                                    <span class="text-md rating-average">{{ product.reviews_average }}/5.0</span>
                                                </div>
                                                {% if product.metafields.custom.rating_breakdown != blank %}
                                                <div class="rating-breakdown">
                                                    {% for rating in product.metafields.custom.rating_breakdown.value %}
                                                    <div class="rating-breakdown-item">
                                                        <div class="rating-score">
                                                            {{ rating.stars }} <i class="icon icon-star"></i>
                                                        </div>
                                                        <div class="rating-bar">
                                                            <div class="value" style="width: {{ rating.percentage }}%;"></div>
                                                        </div>
                                                        <span class="rating-count">{{ rating.count }}</span>
                                                    </div>
                                                    {% endfor %}
                                                </div>
                                                {% endif %}
                                            </div>
                                            <a href="#form-review-{{ block.id }}" class="tf-btn btn-dark2 animate-btn">{{ block.settings.write_review_text }}</a>
                                        </div>
                                        
                                        {% if product.reviews != blank %}
                                        <div class="review-section">
                                            <ul class="review-list">
                                                {% for review in product.reviews limit: block.settings.max_reviews_display %}
                                                <li class="review-item">
                                                    <div class="review-avt">
                                                        {% if review.author_image != blank %}
                                                            <img src="{{ review.author_image }}" alt="{{ review.author }}" width="40" height="40">
                                                        {% else %}
                                                            <div class="review-avatar-placeholder">{{ review.author | first | upcase }}</div>
                                                        {% endif %}
                                                    </div>
                                                    <div class="review-content">
                                                        <div class="review-info">
                                                            <div class="review-meta">
                                                                <span class="review-author fw-medium text-md">{{ review.author }}</span>
                                                                <span class="review-date text-sm">{{ review.created_at | date: '%b %d, %Y' }}</span>
                                                            </div>
                                                            <div class="list-star star-{{ review.rating }}">
                                                                {% for i in (1..5) %}
                                                                    <i class="icon icon-star"></i>
                                                                {% endfor %}
                                                            </div>
                                                        </div>
                                                        <p class="text text-sm text-main-4">{{ review.body }}</p>
                                                    </div>
                                                </li>
                                                {% endfor %}
                                            </ul>
                                        </div>
                                        {% endif %}
                                        
                                        {% if block.settings.show_review_form %}
                                        <form id="form-review-{{ block.id }}" action="{{ routes.root_url }}apps/reviews" method="post" class="form-review">
                                            <h6 class="title">{{ block.settings.write_review_title }}</h6>
                                            <p class="note text-md text-main-4">{{ block.settings.review_form_note }}</p>
                                            
                                            <input type="hidden" name="product_id" value="{{ product.id }}">
                                            
                                            <div class="box-rating">
                                                <span class="text-md">{{ block.settings.your_rating_text }}</span>
                                                <div class="list-rating-check">
                                                    {% for i in (1..5) %}
                                                    <input type="radio" id="star{{ i }}-{{ block.id }}" name="rating" value="{{ i }}" required>
                                                    <label for="star{{ i }}-{{ block.id }}" title="{{ i }} star"></label>
                                                    {% endfor %}
                                                </div>
                                            </div>
                                            
                                            <div class="group-2-ip">
                                                <input type="text" name="author" placeholder="{{ block.settings.name_placeholder }}" required>
                                                <input type="email" name="email" placeholder="{{ block.settings.email_placeholder }}" required>
                                            </div>
                                            
                                            <textarea name="body" id="note-{{ block.id }}" placeholder="{{ block.settings.review_placeholder }}" required></textarea>
                                            
                                            {% if block.settings.show_save_checkbox %}
                                            <div class="check-save">
                                                <input type="checkbox" class="tf-check" id="checksave-{{ block.id }}" name="save_info">
                                                <label for="checksave-{{ block.id }}" class="label text-md">{{ block.settings.save_checkbox_text }}</label>
                                            </div>
                                            {% endif %}
                                            
                                            <button type="submit" class="tf-btn animate-btn">{{ block.settings.submit_button_text }}</button>
                                        </form>
                                        {% endif %}
                                    </div>
                                </div>

                            {% when 'custom_content' %}
                                <div class="tab-pane wd-product-descriptions" id="custom-{{ block.id }}" role="tabpanel">
                                    {{ block.settings.content }}
                                </div>
                        {% endcase %}
                    {% endfor %}
                </div>
            </div>

        {% else %}
            <!-- Horizontal Centered Tab Layout -->
            <div class="flat-animate-tab tab-product-desc">
                <ul class="menu-tab justify-content-md-center" role="tablist">
                    {% for block in section.blocks %}
                        {% case block.type %}
                            {% when 'description' %}
                                {% if product.description != blank %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#descriptions-{{ block.id }}" class="tab-link {% if forloop.first %}active{% endif %}" data-bs-toggle="tab" aria-selected="{% if forloop.first %}true{% else %}false{% endif %}" role="tab">{{ block.settings.title }}</a>
                                </li>
                                {% endif %}
                            {% when 'materials' %}
                                {% if product.metafields.custom.materials_care != blank %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#materials-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                                {% endif %}
                            {% when 'return_policies' %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#returnPolicies-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                            {% when 'additional_info' %}
                                {% if product.metafields.custom.additional_information != blank or block.settings.show_default_info %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#addInformation-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                                {% endif %}
                            {% when 'reviews' %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#reviews-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                            {% when 'custom_content' %}
                                <li class="nav-tab-item" role="presentation">
                                    <a href="#custom-{{ block.id }}" class="tab-link" data-bs-toggle="tab" aria-selected="false" role="tab">{{ block.settings.title }}</a>
                                </li>
                        {% endcase %}
                    {% endfor %}
                </ul>
                
                <div class="tab-content">
                    {% for block in section.blocks %}
                        {% case block.type %}
                            {% when 'description' %}
                                {% if product.description != blank %}
                                <div class="tab-pane {% if forloop.first %}active show{% endif %} wd-product-descriptions" id="descriptions-{{ block.id }}" role="tabpanel">
                                    <div class="item">
                                        <span>{{ product.description }}</span>
                                    </div>
                                </div>
                                {% endif %}

                            {% when 'materials' %}
                                {% if product.metafields.custom.materials_care != blank %}
                                <div class="tab-pane wd-product-descriptions" id="materials-{{ block.id }}" role="tabpanel">
                                    <div class="item">
                                        <p class="fw-medium title">{{ block.settings.subtitle }}</p>
                                        <ul>
                                            {% for care_instruction in product.metafields.custom.materials_care.value %}
                                                <li>{{ care_instruction }}</li>
                                            {% endfor %}
                                        </ul>
                                    </div>
                                </div>
                                {% endif %}

                            {% when 'return_policies' %}
                                <div class="tab-pane wd-product-descriptions" id="returnPolicies-{{ block.id }}" role="tabpanel">
                                    {% if block.settings.policies != blank %}
                                    <ul class="list-policies">
                                        {% assign policies = block.settings.policies | split: '
' %}
                                        {% for policy in policies %}
                                            {% if policy != blank %}
                                            <li>
                                                {{ policy | default: block.settings.default_icon }}
                                            </li>
                                            {% endif %}
                                        {% endfor %}
                                    </ul>
                                    {% endif %}
                                    {% if block.settings.note != blank %}
                                    <p class="text-center text-paragraph">{{ block.settings.note }}</p>
                                    {% endif %}
                                </div>

                            {% when 'additional_info' %}
                                {% if product.metafields.custom.additional_information != blank or block.settings.show_default_info %}
                                <div class="tab-pane wd-product-descriptions wd-vertical-addInformation" id="addInformation-{{ block.id }}" role="tabpanel">
                                    <table class="tb-info-product text-md">
                                        <tbody>
                                            {% if product.metafields.custom.additional_information != blank %}
                                                {% for info in product.metafields.custom.additional_information.value %}
                                                    <tr class="tb-attr-item">
                                                        <th class="tb-attr-label">{{ info.label }}</th>
                                                        <td class="tb-attr-value">
                                                            <p>{{ info.value }}</p>
                                                        </td>
                                                    </tr>
                                                {% endfor %}
                                            {% else %}
                                                {% if product.vendor != blank %}
                                                <tr class="tb-attr-item">
                                                    <th class="tb-attr-label">{{ block.settings.brand_label }}</th>
                                                    <td class="tb-attr-value">
                                                        <p>{{ product.vendor }}</p>
                                                    </td>
                                                </tr>
                                                {% endif %}
                                                
                                                {% if product.type != blank %}
                                                <tr class="tb-attr-item">
                                                    <th class="tb-attr-label">{{ block.settings.type_label }}</th>
                                                    <td class="tb-attr-value">
                                                        <p>{{ product.type }}</p>
                                                    </td>
                                                </tr>
                                                {% endif %}
                                                
                                                {% if product.tags.size > 0 %}
                                                <tr class="tb-attr-item">
                                                    <th class="tb-attr-label">{{ block.settings.tags_label }}</th>
                                                    <td class="tb-attr-value">
                                                        <p>{{ product.tags | join: ', ' }}</p>
                                                    </td>
                                                </tr>
                                                {% endif %}
                                            {% endif %}
                                        </tbody>
                                    </table>
                                </div>
                                {% endif %}

                            {% when 'reviews' %}
                                <div class="tab-pane wd-product-descriptions wd-vertical-review" id="reviews-{{ block.id }}" role="tabpanel">
                                    <div class="wd-customer-review">
                                        <div class="review-heading">
                                            <h6 class="title">{{ block.settings.customer_review_title }}</h6>
                                            <div class="box-rate-review">
                                                <div class="rating-summary">
                                                    <ul class="list-star">
                                                        {% for i in (1..5) %}
                                                            <li><i class="icon icon-star"></i></li>
                                                        {% endfor %}
                                                        <li><span class="count-star text-md">({{ product.reviews_count }})</span></li>
                                                    </ul>
                                                    <span class="text-md rating-average">{{ product.reviews_average }}/5.0</span>
                                                </div>
                                                {% if product.metafields.custom.rating_breakdown != blank %}
                                                <div class="rating-breakdown">
                                                    {% for rating in product.metafields.custom.rating_breakdown.value %}
                                                    <div class="rating-breakdown-item">
                                                        <div class="rating-score">
                                                            {{ rating.stars }} <i class="icon icon-star"></i>
                                                        </div>
                                                        <div class="rating-bar">
                                                            <div class="value" style="width: {{ rating.percentage }}%;"></div>
                                                        </div>
                                                        <span class="rating-count">{{ rating.count }}</span>
                                                    </div>
                                                    {% endfor %}
                                                </div>
                                                {% endif %}
                                            </div>
                                            <a href="#form-review-{{ block.id }}" class="tf-btn btn-dark2 animate-btn">{{ block.settings.write_review_text }}</a>
                                        </div>
                                        
                                        {% if product.reviews != blank %}
                                        <div class="review-section">
                                            <ul class="review-list">
                                                {% for review in product.reviews limit: block.settings.max_reviews_display %}
                                                <li class="review-item">
                                                    <div class="review-avt">
                                                        {% if review.author_image != blank %}
                                                            <img src="{{ review.author_image }}" alt="{{ review.author }}" width="40" height="40">
                                                        {% else %}
                                                            <div class="review-avatar-placeholder">{{ review.author | first | upcase }}</div>
                                                        {% endif %}
                                                    </div>
                                                    <div class="review-content">
                                                        <div class="review-info">
                                                            <div class="review-meta">
                                                                <span class="review-author fw-medium text-md">{{ review.author }}</span>
                                                                <span class="review-date text-sm">{{ review.created_at | date: '%b %d, %Y' }}</span>
                                                            </div>
                                                            <div class="list-star star-{{ review.rating }}">
                                                                {% for i in (1..5) %}
                                                                    <i class="icon icon-star"></i>
                                                                {% endfor %}
                                                            </div>
                                                        </div>
                                                        <p class="text text-sm text-main-4">{{ review.body }}</p>
                                                    </div>
                                                </li>
                                                {% endfor %}
                                            </ul>
                                        </div>
                                        {% endif %}
                                        
                                        {% if block.settings.show_review_form %}
                                        <form id="form-review-{{ block.id }}" action="{{ routes.root_url }}apps/reviews" method="post" class="form-review">
                                            <h6 class="title">{{ block.settings.write_review_title }}</h6>
                                            <p class="note text-md text-main-4">{{ block.settings.review_form_note }}</p>
                                            
                                            <input type="hidden" name="product_id" value="{{ product.id }}">
                                            
                                            <div class="box-rating">
                                                <span class="text-md">{{ block.settings.your_rating_text }}</span>
                                                <div class="list-rating-check">
                                                    {% for i in (1..5) %}
                                                    <input type="radio" id="star{{ i }}-{{ block.id }}" name="rating" value="{{ i }}" required>
                                                    <label for="star{{ i }}-{{ block.id }}" title="{{ i }} star"></label>
                                                    {% endfor %}
                                                </div>
                                            </div>
                                            
                                            <div class="group-2-ip">
                                                <input type="text" name="author" placeholder="{{ block.settings.name_placeholder }}" required>
                                                <input type="email" name="email" placeholder="{{ block.settings.email_placeholder }}" required>
                                            </div>
                                            
                                            <textarea name="body" id="note-{{ block.id }}" placeholder="{{ block.settings.review_placeholder }}" required></textarea>
                                            
                                            {% if block.settings.show_save_checkbox %}
                                            <div class="check-save">
                                                <input type="checkbox" class="tf-check" id="checksave-{{ block.id }}" name="save_info">
                                                <label for="checksave-{{ block.id }}" class="label text-md">{{ block.settings.save_checkbox_text }}</label>
                                            </div>
                                            {% endif %}
                                            
                                            <button type="submit" class="tf-btn animate-btn">{{ block.settings.submit_button_text }}</button>
                                        </form>
                                        {% endif %}
                                    </div>
                                </div>

                            {% when 'custom_content' %}
                                <div class="tab-pane wd-product-descriptions" id="custom-{{ block.id }}" role="tabpanel">
                                    {{ block.settings.content }}
                                </div>
                        {% endcase %}
                    {% endfor %}
                </div>
            </div>
        {% endif %}
    </div>
</section>
<style>
.section-{{ section.id }}-padding {
    padding-top: calc({{ section.settings.padding_top }}px * {{ section.settings.padding_mobile_rate | divided_by: 100.0 }});
    padding-bottom: calc({{ section.settings.padding_bottom }}px * {{ section.settings.padding_mobile_rate | divided_by: 100.0 }});
}
    
@media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
    }
}
</style>
{% schema %}
{
  "name": "t:sections.product-description.name",
  "max_blocks": 10,
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "select",
      "id": "width",
      "label": "t:sections.all.width.label",
      "options": [
        {
          "value": "container",
          "label": "t:sections.all.width.options.boxed"
        },
        {
          "value": "container-full",
          "label": "t:sections.all.width.options.full_width"
        }
      ],
      "default": "container"
    },
    {
      "type": "select",
      "id": "layout",
      "label": "t:sections.product-description.settings.layout.label",
      "options": [
        {
          "value": "accordion",
          "label": "t:sections.product-description.settings.layout.options.accordion"
        },
        {
          "value": "vertical_tab",
          "label": "t:sections.product-description.settings.layout.options.vertical_tab"
        },
        {
          "value": "horizontal_centered_tab",
          "label": "t:sections.product-description.settings.layout.options.horizontal_centered_tab"
        }
      ],
      "default": "accordion"
    },
    {
        "type": "header",
        "content": "t:sections.all.padding.section_padding_heading"
    },
    {
        "type": "range",
        "id": "padding_top",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "t:sections.all.padding.padding_top",
        "default": 0
    },
    {
        "type": "range",
        "id": "padding_bottom",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "t:sections.all.padding.padding_bottom",
        "default": 0
    },
    {
      "type": "range",
      "id": "padding_mobile_rate",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "label": "t:sections.all.padding.padding_mobile_rate.label",
      "default": 75,
      "info": "t:sections.all.padding.padding_mobile_rate.info"
    }
  ],
  "blocks": [
    {
      "type": "description",
      "name": "t:sections.product-description.blocks.description.name",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "t:sections.product-description.blocks.description.settings.paragraph.content"
        },
        {
          "type": "text",
          "id": "title",
          "label": "t:sections.product-description.blocks.description.settings.title.label",
          "default": "t:sections.product-description.blocks.description.settings.title.default"
        }
      ]
    },
    {
      "type": "materials",
      "name": "t:sections.product-description.blocks.materials.name",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "t:sections.product-description.blocks.materials.settings.paragraph.content"
        },
        {
          "type": "text",
          "id": "title",
          "label": "t:sections.product-description.blocks.materials.settings.title.label",
          "default": "t:sections.product-description.blocks.materials.settings.title.default"
        }
      ]
    },
    {
      "type": "return_policies",
      "name": "t:sections.product-description.blocks.return_policies.name",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "t:sections.product-description.blocks.return_policies.settings.paragraph.content"
        },
        {
          "type": "text",
          "id": "title",
          "label": "t:sections.product-description.blocks.return_policies.settings.title.label",
          "default": "t:sections.product-description.blocks.return_policies.settings.title.default"
        },
        {
          "type": "textarea",
          "id": "note",
          "label": "t:sections.product-description.blocks.return_policies.settings.note.label",
          "default": "t:sections.product-description.blocks.return_policies.settings.note.default"
        },
        {
          "type": "textarea",
          "id": "policies",
          "label": "t:sections.product-description.blocks.return_policies.settings.policies.label",
          "info": "t:sections.product-description.blocks.return_policies.settings.policies.info"
        },
        {
          "type": "text",
          "id": "default_icon",
          "label": "t:sections.product-description.blocks.return_policies.settings.default_icon.label",
          "default": "t:sections.product-description.blocks.return_policies.settings.default_icon.default"
        }
      ]
    },
    {
      "type": "additional_info",
      "name": "t:sections.product-description.blocks.additional_info.name",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "t:sections.product-description.blocks.additional_info.settings.paragraph.content"
        },
        {
          "type": "text",
          "id": "title",
          "label": "t:sections.product-description.blocks.additional_info.settings.title.label",
          "default": "t:sections.product-description.blocks.additional_info.settings.title.default"
        },
        {
          "type": "checkbox",
          "id": "show_default_info",
          "label": "t:sections.product-description.blocks.additional_info.settings.show_default_info.label",
          "default": true,
          "info": "t:sections.product-description.blocks.additional_info.settings.show_default_info.info"
        },
        {
          "type": "text",
          "id": "brand_label",
          "label": "t:sections.product-description.blocks.additional_info.settings.brand_label.label",
          "default": "t:sections.product-description.blocks.additional_info.settings.brand_label.default"
        },
        {
          "type": "text",
          "id": "type_label",
          "label": "t:sections.product-description.blocks.additional_info.settings.type_label.label",
          "default": "t:sections.product-description.blocks.additional_info.settings.type_label.default"
        },
        {
          "type": "text",
          "id": "tags_label",
          "label": "t:sections.product-description.blocks.additional_info.settings.tags_label.label",
          "default": "t:sections.product-description.blocks.additional_info.settings.tags_label.default"
        }
      ]
    },
    {
      "type": "reviews",
      "name": "t:sections.product-description.blocks.reviews.name",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "t:sections.product-description.blocks.reviews.settings.paragraph.content"
        },
        {
          "type": "text",
          "id": "title",
          "label": "t:sections.product-description.blocks.reviews.settings.title.label",
          "default": "t:sections.product-description.blocks.reviews.settings.title.default"
        },
        {
          "type": "text",
          "id": "customer_review_title",
          "label": "t:sections.product-description.blocks.reviews.settings.customer_review_title.label",
          "default": "t:sections.product-description.blocks.reviews.settings.customer_review_title.default"
        },
        {
          "type": "text",
          "id": "write_review_text",
          "label": "t:sections.product-description.blocks.reviews.settings.write_review_text.label",
          "default": "t:sections.product-description.blocks.reviews.settings.write_review_text.default"
        },
        {
          "type": "text",
          "id": "write_review_title",
          "label": "t:sections.product-description.blocks.reviews.settings.write_review_title.label",
          "default": "t:sections.product-description.blocks.reviews.settings.write_review_title.default"
        },
        {
          "type": "textarea",
          "id": "review_form_note",
          "label": "t:sections.product-description.blocks.reviews.settings.review_form_note.label",
          "default": "t:sections.product-description.blocks.reviews.settings.review_form_note.default"
        },
        {
          "type": "text",
          "id": "your_rating_text",
          "label": "t:sections.product-description.blocks.reviews.settings.your_rating_text.label",
          "default": "t:sections.product-description.blocks.reviews.settings.your_rating_text.default"
        },
        {
          "type": "text",
          "id": "name_placeholder",
          "label": "t:sections.product-description.blocks.reviews.settings.name_placeholder.label",
          "default": "t:sections.product-description.blocks.reviews.settings.name_placeholder.default"
        },
        {
          "type": "text",
          "id": "email_placeholder",
          "label": "t:sections.product-description.blocks.reviews.settings.email_placeholder.label",
          "default": "t:sections.product-description.blocks.reviews.settings.email_placeholder.default"
        },
        {
          "type": "text",
          "id": "review_placeholder",
          "label": "t:sections.product-description.blocks.reviews.settings.review_placeholder.label",
          "default": "t:sections.product-description.blocks.reviews.settings.review_placeholder.default"
        },
        {
          "type": "checkbox",
          "id": "show_save_checkbox",
          "label": "t:sections.product-description.blocks.reviews.settings.show_save_checkbox.label",
          "default": true
        },
        {
          "type": "textarea",
          "id": "save_checkbox_text",
          "label": "t:sections.product-description.blocks.reviews.settings.save_checkbox_text.label",
          "default": "t:sections.product-description.blocks.reviews.settings.save_checkbox_text.default"
        },
        {
          "type": "text",
          "id": "submit_button_text",
          "label": "t:sections.product-description.blocks.reviews.settings.submit_button_text.label",
          "default": "t:sections.product-description.blocks.reviews.settings.submit_button_text.default"
        },
        {
          "type": "checkbox",
          "id": "show_review_form",
          "label": "t:sections.product-description.blocks.reviews.settings.show_review_form.label",
          "default": true
        },
        {
          "type": "range",
          "id": "max_reviews_display",
          "min": 3,
          "max": 10,
          "step": 1,
          "label": "t:sections.product-description.blocks.reviews.settings.max_reviews_display.label",
          "default": 5
        }
      ]
    },
    {
      "type": "custom_content",
      "name": "t:sections.product-description.blocks.custom_content.name",
      "settings": [
        {
          "type": "paragraph",
          "content": "t:sections.product-description.blocks.custom_content.settings.paragraph.content"
        },
        {
          "type": "text",
          "id": "title",
          "label": "t:sections.product-description.blocks.custom_content.settings.title.label",
          "default": "t:sections.product-description.blocks.custom_content.settings.title.default"
        },
        {
          "type": "richtext",
          "id": "content",
          "label": "t:sections.product-description.blocks.custom_content.settings.content.label",
          "default": "t:sections.product-description.blocks.custom_content.settings.content.default"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.product-description.presets.name",
      "blocks": [
        {
          "type": "description"
        },
        {
          "type": "materials"
        },
        {
          "type": "additional_info"
        }
      ]
    }
  ]
}
{% endschema %}